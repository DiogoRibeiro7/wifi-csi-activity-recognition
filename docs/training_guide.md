# Training Guide

Instructions on how to train models with the provided trainer.

1. Prepare datasets using `datasets` loaders.
2. Configure experiments in YAML files.
3. Run `python -m wifi_activity_recognition.cli train --config config.yaml`.
